/**
 * Defines constants for different action types in a Redux application.
 */

/** Action type for getting characters. */
export const GET_CHARACTERS = "GET_CHARACTERS";

/** Action type for setting characters. */
export const SET_CHARACTERS = "SET_CHARACTERS";

/** Action type for setting the current page. */
export const SET_PAGE = "SET_PAGE";

/** Action type for changing the current page. */
export const CHANGE_PAGE = "CHANGE_PAGE";

/** Action type for adding a favorite character. */
export const ADD_FAVORITE = "ADD_FAVORITE";

/** Action type for removing a favorite character. */
export const REMOVE_FAVORITE = "REMOVE_FAVORITE";

/** Action type for clearing all favorite characters. */
export const CLEAR_FAVORITES = "CLEAR_FAVORITES";

/** Action type for setting an error. */
export const SET_ERROR = "SET_ERROR";

/** Action type for skipping an error. */
export const SKIP_ERROR = "SKIP_ERROR";

/** Action type for setting the loading status. */
export const SET_LOADING = "SET_LOADING";
